This file is a kicad project /media/maerki/shared/octohub4/kicad/octohub4_v0.1/pcb_octohub4.kicad_pro

use python to parse this file, find all schematics (.kicad_sch) and all pcbs (.kicad_pcb).

Now list all symbols you find:
* Count
* Name of symbol
* Path to files which implement the symbol
* Path to the files which reference the symbol

And the same for the footprints.
